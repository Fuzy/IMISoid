<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="imis.client"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <!-- Vlastni poskytovatel obsahu -->
        <provider
            android:exported="false"
            android:name=".persistent.MyEventContentProvider"
            android:authorities="imis.client.events.contentprovider" >
        </provider>

        <!-- Sluzba authentikace -->
        <service
            android:name=".authentication.AuthenticationService"
            android:exported="true" 
            android:permission="normal">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>
        
        <!-- Sluzba synchronizace -->
        <service
            android:name=".syncadapter.SyncService"
            android:exported="true" 
            android:permission="normal">
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>

            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/syncadapter" />
        </service>
        
        <activity
            android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>